{
  "version": "4.0.1",
  "core": {
    "functions": [
      {
        "name": "add_local_rport",
        "detail": "add_local_rport()",
        "doc": "Add 'rport' parameter to the Via header generated by server (see RFC3581 for its meaning). It affects only the current processed request."
      },
      {
        "name": "assert",
        "detail": "assert(statement, [description])",
        "doc": "Only works if [enable_asserts](./Script-CoreParameters.md#enable_asserts) is set to *true*. If the given expression evaluates to *false*, script execution is stopped and the [error_route](./Script-Routes.md#error_route) is executed. If [abort_on_assert](./Script-CoreParameters.md#abort_on_assert) is enabled, OpenSIPS will also shutdown."
      },
      {
        "name": "append_branch_old",
        "detail": "append_branch_old([uri], [qvalue])",
        "doc": "> [!WARNING] > TO BECOME OBSOLETE, replaced by [append_msg_branch()](#append_msg_branchuri-qvalue-flags)."
      },
      {
        "name": "append_msg_branch",
        "detail": "append_msg_branch(uri, [qvalue], [flags])",
        "doc": "Adds a new message branch. The minimal information is the SIP URI of the branch. Optional, a q value may be provided."
      },
      {
        "name": "avp_print",
        "detail": "avp_print()",
        "doc": "Prints the list with all the AVPs from memory. This is only a helper/debug function."
      },
      {
        "name": "cache_store",
        "detail": "cache_store(storage_id, attribute, value, [timeout])",
        "doc": "This sets in a memory-cache-like storage system a new value for an attribute. If the attribute does not already exist in the memcache, it will be inserted with the given value; if already present, its value will be replaced with the new one. The function may optionally take an extra parameter, a timeout (or lifetime) value for the attribute - after the lifetime is exceeded, the attribute is automatically purged from memcache.  If \"timeout\" is omitted or has a value or 0, the attribute/value pair will never expire."
      },
      {
        "name": "cache_remove",
        "detail": "cache_remove(storage_id, attribute)",
        "doc": "This removes an attribute from a memory-cache-like storage system. Function returns false only if the *storage_id* is invalid."
      },
      {
        "name": "cache_fetch",
        "detail": "cache_fetch(storage_id, attribute, result)",
        "doc": "Fetch the value of an attribute from a memory-cache-like storage system. On a successful fetch, the result will be stored in the variable specified by **result_pv**."
      },
      {
        "name": "cache_counter_fetch",
        "detail": "cache_counter_fetch(storage_id, counter_attribute, result)",
        "doc": "This function fetches from a memory-cache-like storage system the value of a counter. The result (if any) will be stored in the variable specified by **result**."
      },
      {
        "name": "cache_add",
        "detail": "cache_add( storage_id, attribute, increment, expire, [new_val])",
        "doc": "This increments an attribute in a memory-cache-like storage system that supports such an operation. If the attribute does not exit, it will be created with the value of **increment**."
      },
      {
        "name": "cache_sub",
        "detail": "cache_sub(storage_id, attribute, decrement, expire, [new_val])",
        "doc": "This decrements an attribute in a memory-cache-like storage system that supports such an operation."
      },
      {
        "name": "cache_raw_query",
        "detail": "cache_raw_query(storage_id, raw_query, result)",
        "doc": "The function runs the provided raw query (in the back-end dependent language) and returns the results (if any) in the AVP (or AVP list) provided in *result*. This parameter may be missing, if the query returns no results."
      },
      {
        "name": "break",
        "detail": "break()",
        "doc": "Since v0.10.0-dev3, 'break' can no longer be used to stop the execution of a route. The only place to use is to end a 'case' block in a 'switch' statement. 'return' must be now used instead of old 'break'."
      },
      {
        "name": "construct_uri",
        "detail": "construct_uri(proto,[user],domain,[port],[extra],result)",
        "doc": "The function builds a valid sip uri based on the arguments it receives. The result (if any) will be stored in the **result** AVP variable. If you want to omit a part of the sip uri, just omit the respective parameter."
      },
      {
        "name": "drop",
        "detail": "drop()",
        "doc": "Stop the execution of the configuration script and alter the implicit action which is done afterwards."
      },
      {
        "name": "exit",
        "detail": "exit()",
        "doc": "Stop the execution of the configuration script -- it has the same behaviour as return(0). It does not affect the implicit action to be taken after script execution."
      },
      {
        "name": "force_rport",
        "detail": "force_rport()",
        "doc": "Force_rport() adds the rport parameter to the first Via header. Thus, **OpenSIPS** will add the received IP port to the top most via header in the SIP message, even if the client does not indicate support for rport. This enables subsequent SIP messages to return to the proper port later on in a SIP transaction."
      },
      {
        "name": "force_send_socket",
        "detail": "force_send_socket(proto:address[:port])",
        "doc": "Force **OpenSIPS** to send the message from the specified socket (it _must_ be one of the sockets **OpenSIPS** listens on). If the protocol doesn't match (e.g. UDP message \"forced\" to a TCP socket) the closest socket of the same protocol is used."
      },
      {
        "name": "force_tcp_alias",
        "detail": "force_tcp_alias([port_alias])",
        "doc": "Enables TCP connection reusage (RFC 5923) for the current TLS (or WSS, TCP, WS) connection (source IP + **source port** + transport), regardless if the Via header field contains an *\";alias\"* parameter or not. All backwards SIP requests, towards the same (source IP + **Via port** + transport) pair will be forced over this connection, for as long as it stays open. The main purpose of this function (and of RFC 5923) is to minimize the number of TLS connections a SIP proxy must set up, due to the significant CPU overhead of the TLS cipher negotiation phase."
      },
      {
        "name": "tcp_close_conn",
        "detail": "tcp_close_conn(ipport)",
        "doc": "Force-close an existing TCP-based connection. The *ipport* argument may be a TCP connection ID, as reported by the `tcp:list` MI command, or a remote endpoint in the form `ip:port` or `proto:ip:port`. If the protocol is omitted, OpenSIPS searches all TCP-based transports."
      },
      {
        "name": "forward",
        "detail": "forward(destination)",
        "doc": "Forward the SIP request to the given destination in stateless mode. This has the format of [proto:]host[:port]. Host can be an IP or hostname; supported protocols are UDP, TCP and TLS. (For TLS, you need to compile the TLS support into core). If proto or port are not specified, NAPTR and SRV lookups will be used to determine them (if possible)."
      },
      {
        "name": "get_timestamp",
        "detail": "get_timestamp(sec_avp,usec_avp)",
        "doc": "Returns the current timestamp, seconds and microseconds of the current second, from a single system call."
      },
      {
        "name": "isdsturiset",
        "detail": "isdsturiset()",
        "doc": "Test if the dst_uri field (next hop address) is set."
      },
      {
        "name": "isflagset",
        "detail": "isflagset(string)",
        "doc": "Test if a flag is set for currently processed message."
      },
      {
        "name": "isbflagset",
        "detail": "isbflagset(flag, [branch_idx])",
        "doc": "Test if a flag is set for a specific branch. \"branch_idx\" identifies the branch for which the flags are tested - it must be a positive number. Branch index 0 refers to the RURI branch. If this parameter is missing, 0 branch index is used as default."
      },
      {
        "name": "is_myself",
        "detail": "is_myself(host, [port])",
        "doc": "Test if the host and optionally the port represent one of the addresses that OpenSIPS listens on. This checks the list of local IP addresses, hostnames and aliases that have been set in the OpenSIPS configuration file."
      },
      {
        "name": "log",
        "detail": "log([level,] string)",
        "doc": "Write text message to standard error terminal or syslog. You can specify the log level as first parameter."
      },
      {
        "name": "move_msg_branch",
        "detail": "move_msg_branch([src_idx], [dst_idx] [, keep])",
        "doc": "Moves the whole information attached to the **src_idx** branch to the **dst_idx** branch. Both **src_idx** and **dst_idx** should be an integer value and should represent a valid branch index. If they are not provided, or have a negative value, the main/message branch is considered. By default, the function removes the **src_idx** branch after moving it, and shifts all the remaining branches after it. If, however, the third parameter of the function is the **keep** string, the branch is not removed, and only copied to the **dst_idx** branch."
      },
      {
        "name": "next_branches",
        "detail": "next_branches()",
        "doc": "Adds to the request a new destination set that includes all highest priority class contacts ('q' value based) from the serialized branches (see serialize_branches()). If called from a route block, it rewrites the request uri with first contact and adds the remaining contacts as parallel branches.  If called from failure route block, adds all contacts as parallel branches.  All used contacts are removes the serialized branches."
      },
      {
        "name": "prefix",
        "detail": "prefix(str)",
        "doc": "Add the string parameter in front of username in R-URI."
      },
      {
        "name": "pv_printf",
        "detail": "pv_printf(pv, fmt_str)",
        "doc": "Prints the formatted string 'fmt_str' in the AVP 'pv'. The 'fmt_str' parameter can include any pseudo-variable defined in **OpenSIPS**. The 'pv' can be any writable pseudo-variable -- e.g.,: AVPs, VARs, `$ru`, `$rU`, `$rd`, `$du`, `$br`, `$fs.`"
      },
      {
        "name": "raise_event",
        "detail": "raise_event(event, [attrs], [vals])",
        "doc": "Raises from script an event through OpenSIPS Event Interface."
      },
      {
        "name": "remove_msg_branch",
        "detail": "remove_msg_branch(branch_idx)",
        "doc": "Removes a given branch. Once a branch is removed, all the subsequent branches are shifted (i.e. if branch n is removed, then the old n+1 branch becomes the new n branch, the old n+2 branch becomes n+1 and so on)."
      },
      {
        "name": "return",
        "detail": "return(int)",
        "doc": "The return() function allows you to return any integer value from a called route() block. You can test the value returned by a route using \"`$retcode`\" variable."
      },
      {
        "name": "resetdsturi",
        "detail": "resetdsturi()",
        "doc": "Set the value of dst_uri filed to NULL. dst_uri field is usually set after loose_route() or lookup(\"location\") if the contact address is behind a NAT."
      },
      {
        "name": "resetflag",
        "detail": "resetflag(flag)",
        "doc": "Reset a flag for currently processed message (unset its value)."
      },
      {
        "name": "resetbflag",
        "detail": "resetbflag(flag, [branch_idx])",
        "doc": "Reset a flag for a specific branch (unset its value). \"branch_idx\" identifies the branch for which the flag is reset - it must be a positive number. Branch index 0 refers to the RURI branch. If this parameter is missing, 0 branch index is used as default."
      },
      {
        "name": "revert_uri",
        "detail": "revert_uri()",
        "doc": "Set the R-URI to the value of the R-URI as it was when the request was received by server (undo all changes of R-URI)."
      },
      {
        "name": "set_via_handling",
        "detail": "set_via_handling(flags)",
        "doc": "Rewrite the domain part of the R-URI with the value of function's parameter. Other parts of the R-URI like username, port and URI parameters remain unchanged."
      },
      {
        "name": "sethost",
        "detail": "sethost(host)",
        "doc": "Rewrite the domain part of the R-URI with the value of function's parameter. Other parts of the R-URI like username, port and URI parameters remain unchanged."
      },
      {
        "name": "sethostport",
        "detail": "sethostport(hostport)",
        "doc": "Rewrite the domain part and port of the R-URI with the value of function's parameter. Other parts of the R-URI like username and URI parameters remain unchanged."
      },
      {
        "name": "setuser",
        "detail": "setuser(user)",
        "doc": "Rewrite the user part of the R-URI with the value of function's parameter."
      },
      {
        "name": "setuserpass",
        "detail": "setuserpass(pass)",
        "doc": "Rewrite the password part of the R-URI with the value of function's parameter."
      },
      {
        "name": "setport",
        "detail": "setport(port)",
        "doc": "Rewrites/sets the port part of the R-URI with the value of function's parameter."
      },
      {
        "name": "seturi",
        "detail": "seturi(str)",
        "doc": "Rewrite the request URI."
      },
      {
        "name": "route",
        "detail": "route(name [, param1 [, param2 [, ...] ] ] )",
        "doc": "This function is used to run the code from the 'name' route, declared in the script. Optionally, it can receive several parameters (up to 7), that can be later retrieved using the '`$param(idx)`' pseudo-variable."
      },
      {
        "name": "script_trace",
        "detail": "script_trace([log_level, pv_format_string, [info]])",
        "doc": "This function start the script tracing - this helps to better understand the flow of execution in the OpenSIPS script, like what function is executed, what line it is, etc. Moreover, you can also trace the values of pseudo-variables, as script execution progresses."
      },
      {
        "name": "send",
        "detail": "send(destination [, headers])",
        "doc": "Send the original SIP message to a specific destination in stateless mode. This is definied as [proto:]host[:port]. No changes are applied to received message, no Via header is added, unless headers parameter is specified. Host can be an IP or hostname; supported protocols are UDP, TCP and TLS. (For TLS, you need to compile the TLS support into core). If proto or port are not specified, NAPTR and SRV lookups will be used to determine them (if possible). The headers parameter should end in '\\r\\n'."
      },
      {
        "name": "serialize_branches",
        "detail": "serialize_branches(clear_previous[, keep_order])",
        "doc": "Takes all currently added branches for parallel forking (e.g. with lookup() or append_msg_branch()), as well as the current branch (R-URI (`$ru`) / outbound Proxy (`$du`) / q value (`$ru_q`) / branch flags / forced Path headers / forced send socket), and prepares them for serial forking instead. The ordering is done in decreasing \"q\" order. The serialized branches are internally stored in the \"`$avp(serial_branch)`\" AVP - this allows them to be manipulated by the \"next_branches()\" function, usually within a failure route."
      },
      {
        "name": "set_advertised_address",
        "detail": "set_advertised_address(adv_addr)",
        "doc": "Same as 'advertised_address' but it affects only the current message. It has priority if 'advertised_address' is also set."
      },
      {
        "name": "set_advertised_port",
        "detail": "set_advertised_port(adv_port)",
        "doc": "Same as 'advertised_port' but it affects only the current message. It has priority over 'advertised_port'."
      },
      {
        "name": "setdsturi",
        "detail": "setdsturi(uri)",
        "doc": "Explicitely set the dst_uri field to the value of the paramater. The parameter has to be a valid SIP URI."
      },
      {
        "name": "setflag",
        "detail": "setflag(flag)",
        "doc": "Set a flag for currently processed message. The flags are used to mark the message for special processing (e.g. pinging NAT'ed contacts, TCP connect behavior, etc.) or to keep some state (e.g. message authenticated). The OpenSIPS script supports, at most, 32 unique string flags."
      },
      {
        "name": "setbflag",
        "detail": "setbflag(flag, [branch_idx])",
        "doc": "Set a flag for a specific branch. \"branch_idx\" identifies the branch for which the flag is set - it must be a positive number. Branch index 0 refers to the RURI branch. If this parameter is missing, 0 branch index is used as default.  The OpenSIPS script supports, at most, 32 unique string branch flags."
      },
      {
        "name": "socket_belongs_to_bond",
        "detail": "socket_belongs_to_bond( socket, bond)",
        "doc": "This function checks if a SIP socket is part of a certain bond socket (see the [definition of a bond socket](Script-CoreParameters.md#socket))."
      },
      {
        "name": "sr_check_status",
        "detail": "sr_check_status( group, [identifier])",
        "doc": "Function to check the status of an 'status/report' identifier. Such checking is very useful for determining at script level the readiness of a module or core component (if able to provide its full functionality)."
      },
      {
        "name": "strip",
        "detail": "strip(n)",
        "doc": "Strip the first N-th characters from username of R-URI (N is the value of the parameter)."
      },
      {
        "name": "strip_tail",
        "detail": "strip_tail(n)",
        "doc": "Strip the last N-th characters from username of R-URI (N is the value of the parameter)."
      },
      {
        "name": "subscribe_event",
        "detail": "subscribe_event(string, string [, int])",
        "doc": "Subscribes an external application for a certain event for the OpenSIPS Event Interface. This is used for transport protocols that cannot subscribe by themselves (example event_rabbitmq). This function should be called only once in the startup_route if the subscription doesn't expire, or in a timer route if the subscription should be renewed once in a while."
      },
      {
        "name": "swap_msg_branches",
        "detail": "swap_msg_branches([br1_idx], [br2_idx])",
        "doc": "Swaps the information between two branches, represented by the **br1_idx** and **br2_idx**. Both values should be an integer value and should represent a valid branch index. If they are not provided, or have a negative value, the main/message branch is considered."
      },
      {
        "name": "use_blacklist",
        "detail": "use_blacklist(bl_name)",
        "doc": "Enables the DNS blacklist name received as parameter. Its primary purposes will be to prevent sending requests to critical IPs (like GWs) due DNS or to avoid sending to destinations that are known to be unavailable (temporary or permanent)."
      },
      {
        "name": "unuse_blacklist",
        "detail": "unuse_blacklist(bl_name)",
        "doc": "Disables the DNS blacklist name received as parameter."
      },
      {
        "name": "check_blacklist_rule",
        "detail": "check_blacklist_rule([bl_name], ip[, port [, proto]])",
        "doc": "Checks whether a specific proto:ip:port+pattern matches a blacklist, or all if *bl_name* is not specified."
      },
      {
        "name": "add_blacklist_rule",
        "detail": "add_blacklist_rule([bl_name], ip[, port [, proto [, expire]]])",
        "doc": "Adds a proto:ip:port+pattern rule to a blacklist."
      },
      {
        "name": "del_blacklist_rule",
        "detail": "del_blacklist_rule([bl_name], ip[, port [, proto]])",
        "doc": "Removes a proto:ip:port+pattern rule from a blacklist."
      },
      {
        "name": "xlog",
        "detail": "xlog([log_level, ]format_string)",
        "doc": "Allows various debugging / runtime / critical messages to be printed as the execution of the OpenSIPS script is done. All pseudo-variables included in the *format_string* parameter will be expanded. There are several optional logging levels which can be specified. They work in accordance with the severity levels of syslog. The levels are named as follows:"
      }
    ],
    "params": [
      {
        "name": "abort_on_assert",
        "detail": "core parameter",
        "doc": "Default value is `false`."
      },
      {
        "name": "advertised_address",
        "detail": "core parameter",
        "doc": "This can be an IP address or string and represents the address advertised in the Via header and other destination lumps, such as Record-Route headers. If empty or not set, the socket address used to send the request is advertised."
      },
      {
        "name": "advertised_port",
        "detail": "core parameter",
        "doc": "The port advertised in the Via header and other destination lumps, such as Record-Route headers. If empty or not set, the port used to send the message is advertised. The same warnings as for [advertised_address](#advertised_address) apply."
      },
      {
        "name": "alias",
        "detail": "core parameter",
        "doc": "Sets alias hostnames for the server. It can be set multiple times, with each value added to the list used to match the hostname when `myself` is checked."
      },
      {
        "name": "auto_aliases",
        "detail": "core parameter",
        "doc": "This parameter controls whether aliases should be automatically discovered and added while fixing listening sockets. The auto-discovered aliases are the result of a DNS lookup, when the [socket](#socket) definition uses a hostname, or of a reverse DNS lookup on the socket IP."
      },
      {
        "name": "auto_scaling_cycle",
        "detail": "core parameter",
        "doc": "The number of seconds defining an auto-scaling cycle. During each cycle, the auto-scaling engine evaluates the internal load of the process groups and decides whether more processes need to be created or existing processes need to be terminated. See [auto_scaling_profile](#auto_scaling_profile) for more details on how auto-scaling works."
      },
      {
        "name": "auto_scaling_profile",
        "detail": "core parameter",
        "doc": "Defines the auto-scaling behavior: how many processes are allowed and when to create or terminate processes. These profiles may be used for UDP processes (see the [udp_workers](#udp_workers) or [socket](#socket) options), TCP processes (see the [tcp_workers](#tcp_workers) option) or timer processes (see the [timer_workers](#timer_workers) option)."
      },
      {
        "name": "check_via",
        "detail": "core parameter",
        "doc": "Checks whether the address in the topmost Via header of replies is local. Default value is `false` (check disabled)."
      },
      {
        "name": "chroot",
        "detail": "core parameter",
        "doc": "The value must be a valid path in the system. If set, **OpenSIPS** will chroot, changing its root directory to this path."
      },
      {
        "name": "debug_mode",
        "detail": "core parameter",
        "doc": "Enabling **debug_mode** is a fast way to debug **OpenSIPS**. This option automatically forces: * foreground mode (do not detach from the console) * logging level 4 (debug) * logging to standard error * core dumping * UDP worker processes to 2, if UDP is enabled * TCP worker processes to 2, or the maximum configured value if lower than 2, if TCP is enabled"
      },
      {
        "name": "db_version_table",
        "detail": "core parameter",
        "doc": "The name of the database table used by the DB API to check table versions."
      },
      {
        "name": "db_default_url",
        "detail": "core parameter",
        "doc": "The default DB URL used by modules when no per-module URL is configured."
      },
      {
        "name": "db_max_async_connections",
        "detail": "core parameter",
        "doc": "Maximum number of TCP connections opened from a single OpenSIPS worker to each individual SQL backend."
      },
      {
        "name": "disable_503_translation",
        "detail": "core parameter",
        "doc": "If set to `true`, OpenSIPS will not translate received 503 replies into 500 replies. RFC 3261 states that a proxy should never relay a 503 response, but transform it into a 500 response instead."
      },
      {
        "name": "disable_core_dump",
        "detail": "core parameter",
        "doc": "If set to `true`, OpenSIPS disables core dumps by setting the core dump size limit to 0. By default, core dump limits are set to unlimited or to a high enough value."
      },
      {
        "name": "disable_dns_blacklist",
        "detail": "core parameter",
        "doc": "When DNS failover is configured, the DNS resolver can temporarily blacklist failed destinations. This prevents **OpenSIPS**, for a limited time, from sending requests to destinations known to have failed. The blacklist acts as a memory for the DNS resolver."
      },
      {
        "name": "disable_dns_failover",
        "detail": "core parameter",
        "doc": "If set to `true`, OpenSIPS disables DNS-based failover. This is a global option, affecting both the core and the modules."
      },
      {
        "name": "disable_stateless_fwd",
        "detail": "core parameter",
        "doc": "Controls the handling of stateless replies:"
      },
      {
        "name": "dns",
        "detail": "core parameter",
        "doc": "This parameter controls whether the SIP server should attempt to look up its own domain name in DNS. If this parameter is set to `true` and the domain name is not in DNS, a warning is printed to syslog and a `received=` field is added to the Via header."
      },
      {
        "name": "dns_retr_time",
        "detail": "core parameter",
        "doc": "Time in seconds before retrying a DNS request. Default value is system-specific and also depends on the `/etc/resolv.conf` content, usually 5 seconds."
      },
      {
        "name": "dns_retr_no",
        "detail": "core parameter",
        "doc": "Number of DNS retransmissions before giving up. Default value is system-specific and also depends on the `/etc/resolv.conf` content, usually `4`."
      },
      {
        "name": "dns_servers_no",
        "detail": "core parameter",
        "doc": "How many DNS servers from `/etc/resolv.conf` will be used."
      },
      {
        "name": "dns_try_ipv6",
        "detail": "core parameter",
        "doc": "If set to `true` and a DNS lookup fails, OpenSIPS retries the lookup for IPv6 using an AAAA record."
      },
      {
        "name": "dns_try_naptr",
        "detail": "core parameter",
        "doc": "Controls whether NAPTR lookups are performed when doing DNS-based routing for SIP requests. If disabled, DNS lookup starts with SRV lookups."
      },
      {
        "name": "dns_use_search_list",
        "detail": "core parameter",
        "doc": "If set to `false`, the search list in `/etc/resolv.conf` is ignored, which means fewer lookups and faster DNS failure handling."
      },
      {
        "name": "dst_blacklist",
        "detail": "core parameter",
        "doc": "Defines an IP/destination blacklist. These lists can be selected from the script, at runtime, to filter outgoing requests based on IP, protocol, port, etc."
      },
      {
        "name": "enable_asserts",
        "detail": "core parameter",
        "doc": "Default value is `false`."
      },
      {
        "name": "event_pkg_threshold",
        "detail": "core parameter",
        "doc": "A number representing the percentage threshold above which the E_CORE_PKG_THRESHOLD event is raised, warning about a low amount of free private memory. It accepts integer values between `0` and `100`."
      },
      {
        "name": "event_shm_threshold",
        "detail": "core parameter",
        "doc": "A number representing the percentage threshold above which the E_CORE_SHM_THRESHOLD event is raised, warning about a low amount of free shared memory. It accepts integer values between `0` and `100`."
      },
      {
        "name": "exec_dns_threshold",
        "detail": "core parameter",
        "doc": "A number representing the maximum number of microseconds a DNS query is expected to take. Anything above the set number triggers a warning message to the logging facility."
      },
      {
        "name": "exec_msg_threshold",
        "detail": "core parameter",
        "doc": "A number representing the maximum number of microseconds the processing of a SIP message is expected to take. Anything above the set number triggers a warning message to the logging facility. Aside from the message and the processing time, the most time-consuming function calls from the script are also logged."
      },
      {
        "name": "include_file",
        "detail": "core parameter",
        "doc": "Can be called outside route blocks to load additional routes or blocks, or inside route blocks to load additional script actions. The file path can be relative or absolute. If the path is relative, OpenSIPS first tries to locate it relative to the directory from which OpenSIPS was started. If that fails, it tries the directory of the file that includes it. An error is raised if the file is not found."
      },
      {
        "name": "import_file",
        "detail": "core parameter",
        "doc": "Alias for [include_file](#include_file)."
      },
      {
        "name": "log_event_enabled",
        "detail": "core parameter",
        "doc": "Enables the E_CORE_LOG event for every log message generated by OpenSIPS."
      },
      {
        "name": "log_event_level_filter",
        "detail": "core parameter",
        "doc": "Extra log level filtering for the E_CORE_LOG event. This parameter is useful when different verbosity levels are desired between syslog/standard error logs and the logs delivered through E_CORE_LOG."
      },
      {
        "name": "log_json_buf_size",
        "detail": "core parameter",
        "doc": "Size of the buffer used for printing the JSON document corresponding to a log message. This parameter makes sense when the `json` or `json_cee` log formats are used. If the buffer is too small, the log message is truncated."
      },
      {
        "name": "log_level",
        "detail": "core parameter",
        "doc": "Sets the logging level, controlling how verbose OpenSIPS should be. Higher values make **OpenSIPS** print more messages."
      },
      {
        "name": "log_msg_buf_size",
        "detail": "core parameter",
        "doc": "Size of the buffer used for printing the log message payload. This is used for printing the `message` field from a JSON document when the `json` or `json_cee` log formats are used, or when the E_CORE_LOG event is raised, if enabled. If the buffer is too small, the log message is truncated."
      },
      {
        "name": "log_stdout",
        "detail": "core parameter",
        "doc": "Controls whether OpenSIPS preserves standard output. This may be useful when trying to extract logs from third-party libraries."
      },
      {
        "name": "log_prefix",
        "detail": "core parameter",
        "doc": "A string prefix prepended to all logs produced by OpenSIPS, from both C code and script `xlog()` statements. A non-empty value automatically gets a trailing `:`."
      },
      {
        "name": "max_while_loops",
        "detail": "core parameter",
        "doc": "Sets the maximum number of loop iterations allowed within a `while` statement. This protects against infinite loops during configuration script execution."
      },
      {
        "name": "maxbuffer",
        "detail": "core parameter",
        "doc": "The maximum receive buffer size, in bytes, that OpenSIPS will accept during the auto-probing procedure used to discover the maximum buffer size for receiving UDP messages."
      },
      {
        "name": "mem-group",
        "detail": "core parameter",
        "doc": "Defines a group of modules, by name, for separate memory statistics. OpenSIPS provides per-group memory information: number of allocated fragments, amount of used memory and amount of real used memory, including memory manager overhead. This is useful when monitoring memory usage for a specific module or group of modules."
      },
      {
        "name": "mem_warming",
        "detail": "core parameter",
        "doc": "Only relevant when the `HP_MALLOC` compile flag is enabled. If set to `true`, on each startup, OpenSIPS attempts to restore the memory fragmentation pattern it had before the stop/restart. If no [mem_warming_pattern_file](#mem_warming_pattern_file) from a previous run is found, memory warming is skipped and the memory allocator starts with a large memory chunk, like all other allocators."
      },
      {
        "name": "mem_warming_percentage",
        "detail": "core parameter",
        "doc": "How much of OpenSIPS memory should be fragmented with the pattern of the previous run, upon restart. Used at startup if [mem_warming](#mem_warming) is enabled."
      },
      {
        "name": "mem_warming_pattern_file",
        "detail": "core parameter",
        "doc": "Only relevant if [mem_warming](#mem_warming) is enabled. It contains the memory fragmentation pattern of a previous OpenSIPS run. This file is overwritten during each OpenSIPS shutdown and is used during startup in order to restore service behavior as soon as possible."
      },
      {
        "name": "mcast_loopback",
        "detail": "core parameter",
        "doc": "If set to `true`, multicast datagrams are sent over loopback."
      },
      {
        "name": "mcast_ttl",
        "detail": "core parameter",
        "doc": "Sets the multicast TTL."
      },
      {
        "name": "mhomed",
        "detail": "core parameter",
        "doc": "If set to `true`, OpenSIPS tries to locate the outbound interface on multihomed hosts. This lookup is time-consuming, so it is disabled by default."
      },
      {
        "name": "mpath",
        "detail": "core parameter",
        "doc": "Sets the module search path. This can be used to simplify `loadmodule` statements."
      },
      {
        "name": "open_files_limit",
        "detail": "core parameter",
        "doc": "If set and greater than the current open file limit, **OpenSIPS** tries to increase its open file limit to this number. **OpenSIPS** must be started as root in order to increase a limit past the hard limit, which is `1024` on most systems for open files."
      },
      {
        "name": "poll_method",
        "detail": "core parameter",
        "doc": "The poll method used by the internal I/O reactor. By default, the best method for the current OS is selected."
      },
      {
        "name": "pv_print_buf_size",
        "detail": "core parameter",
        "doc": "The maximum size, in bytes, of an expanded formatted string containing variables or pseudo-variables."
      },
      {
        "name": "query_buffer_size",
        "detail": "core parameter",
        "doc": "If set to a value greater than `1`, DB inserts are not flushed one by one. Rows to be inserted are kept in memory until they gather up to `query_buffer_size` rows, and only then are they flushed to the database."
      },
      {
        "name": "query_flush_time",
        "detail": "core parameter",
        "doc": "If [query_buffer_size](#query_buffer_size) is set to a value greater than `1`, a timer triggers once every `query_flush_time` seconds, ensuring that no row is kept in memory for too long."
      },
      {
        "name": "restart_persistency_cache_file",
        "detail": "core parameter",
        "doc": "The name of the cache file used to store restart persistency memory."
      },
      {
        "name": "restart_persistency_size",
        "detail": "core parameter",
        "doc": "The size of the restart persistency cache file, in megabytes. If this parameter is not specified, it defaults to the size of the shared memory."
      },
      {
        "name": "rev_dns",
        "detail": "core parameter",
        "doc": "Controls whether the SIP server should attempt to look up its own IP address in DNS. If this parameter is set to `true` and the IP address is not in DNS, a warning is printed to syslog and a `received=` field is added to the Via header."
      },
      {
        "name": "server_header",
        "detail": "core parameter",
        "doc": "The body of the Server header field generated by **OpenSIPS** when it sends a reply as UAS."
      },
      {
        "name": "server_signature",
        "detail": "core parameter",
        "doc": "Controls whether the Server header is added to locally generated messages."
      },
      {
        "name": "shm_hash_split_percentage",
        "detail": "core parameter",
        "doc": "Only relevant when the `HP_MALLOC` compile flag is enabled. It controls how many memory buckets are optimized. For example, setting it to `2` optimizes the first `2%` of the most frequently used buckets."
      },
      {
        "name": "shm_memlog_size",
        "detail": "core parameter",
        "doc": "Configures the maximum number of shared memory operations to keep in the in-memory history. A separate memory block, dedicated to this shared memory debug information, is allocated. As a result, **OpenSIPS** uses more system memory than the configured shared memory pool, set with the `-m` command-line option. For example, `shm_memlog_size = 1000000` uses approximately 750 MB more memory. This option is intended for debugging."
      },
      {
        "name": "shm_secondary_hash_size",
        "detail": "core parameter",
        "doc": "Only relevant when the `HP_MALLOC` compile flag is enabled. It represents the optimization factor of a single bucket. For example, setting it to `4` causes optimized buckets to be further split into `4`."
      },
      {
        "name": "sip_warning",
        "detail": "core parameter",
        "doc": "If set to `true`, a Warning header is added to each reply generated by **OpenSIPS**. The header contains details that help troubleshooting using network traffic dumps."
      },
      {
        "name": "socket",
        "detail": "core parameter",
        "doc": "Sets the network addresses/sockets the OpenSIPS server should listen on. Its syntax is `protocol:address[:port|portrange]`, where: * **protocol**: one of the transport modules loaded in the configuration file, such as `udp`, `tcp`, `tls`, `bin` or `hep`. * **address**: an IP address, hostname, network interface name or the `*` wildcard, which makes OpenSIPS listen on all possible interfaces for that protocol. * **port**: optional listening socket port; if absent, the default port exported by the transport module is used. * **portrange**: optional set of ports that should listen for the same IP address."
      },
      {
        "name": "stderror_enabled",
        "detail": "core parameter",
        "doc": "Enables writing log messages to standard error."
      },
      {
        "name": "stderror_level_filter",
        "detail": "core parameter",
        "doc": "Extra log level filtering for messages written to standard error. This parameter is useful when different verbosity levels are desired for syslog and standard error logging."
      },
      {
        "name": "syslog_enabled",
        "detail": "core parameter",
        "doc": "Enables writing log messages to syslog."
      },
      {
        "name": "stderror_log_format",
        "detail": "core parameter",
        "doc": "Format of the log messages printed to standard error. Possible values are: * `plain_text`: standard plain-text log message. * `json`: basic JSON document. * `json_cee`: JSON document following the [CEE (Common Event Expression)](https://cee.mitre.org/language/1.0-beta1/core-profile.html) schema."
      },
      {
        "name": "syslog_facility",
        "detail": "core parameter",
        "doc": "If **OpenSIPS** logs to syslog, this parameter controls the syslog facility. It is useful when diverting all **OpenSIPS** logs to a different log file. See `syslog(3)` for more details."
      },
      {
        "name": "syslog_level_filter",
        "detail": "core parameter",
        "doc": "Extra log level filtering for messages sent to syslog. This parameter is useful when different verbosity levels are desired for syslog and standard error logging."
      },
      {
        "name": "syslog_log_format",
        "detail": "core parameter",
        "doc": "Format of the log messages sent to syslog. Possible values are: * `plain_text`: standard plain-text log message. * `json`: basic JSON document. * `json_cee`: JSON document following the [CEE (Common Event Expression)](https://cee.mitre.org/language/1.0-beta1/core-profile.html) schema."
      },
      {
        "name": "syslog_name",
        "detail": "core parameter",
        "doc": "Sets the identifier printed in syslog. The value must be a string and has effect only when **OpenSIPS** runs in daemon mode, after daemonizing."
      },
      {
        "name": "tcp_workers",
        "detail": "core parameter",
        "doc": "Number of worker processes used for parsing and processing messages received over TCP connections. The socket read/write I/O is handled separately by the TCP thread pool, configured through [tcp_threads](#tcp_threads). These workers handle traffic over any TCP-based protocol, such as SIP-TCP, SIP-TLS, SIP-WS, SIP-WSS, BIN or HEP."
      },
      {
        "name": "tcp_threads",
        "detail": "core parameter",
        "doc": "Number of TCP I/O threads used by the TCP pool. If this parameter is not set, OpenSIPS uses the number of online CPUs. If the CPU count cannot be detected, it falls back to [tcp_workers](#tcp_workers), or to `1` if no TCP workers are configured."
      },
      {
        "name": "tcp_accept_aliases",
        "detail": "core parameter",
        "doc": "If set to `true`, OpenSIPS enforces RFC 5923 behavior when detecting an `;alias` Via header field parameter, and reuses any TCP, TLS, WS or WSS connection opened for such SIP requests when sending other SIP requests backwards towards the same source IP, Via port and protocol tuple. The purpose of RFC 5923 is to minimize the number of TLS connections a SIP proxy must open, due to the large CPU overhead of connection setup."
      },
      {
        "name": "tcp_connect_timeout",
        "detail": "core parameter",
        "doc": "Time in milliseconds before an ongoing blocking connection attempt is aborted."
      },
      {
        "name": "tcp_connection_lifetime",
        "detail": "core parameter",
        "doc": "Lifetime in seconds for TCP sessions. TCP sessions inactive for more than `tcp_connection_lifetime` seconds are closed by **OpenSIPS**. Setting this value to `0` closes TCP connections quickly. You can also set the TCP lifetime to the expire value of the REGISTER by using the `tcp_persistent_flag` parameter of the registrar module."
      },
      {
        "name": "tcp_max_connections",
        "detail": "core parameter",
        "doc": "Maximum number of active TCP accepted connections, meaning connections initiated by remote endpoints. Once the limit is reached, new incoming TCP connections are rejected. There is currently no limit for outgoing TCP connections initiated by OpenSIPS."
      },
      {
        "name": "tcp_max_msg_time",
        "detail": "core parameter",
        "doc": "The maximum number of seconds that a SIP message is expected to take to fully arrive over TCP. If a single SIP packet is still not fully received after this number of seconds, the connection is dropped. This may happen when the connection is overloaded and traffic is highly fragmented, or during attacks that intentionally fragment traffic in order to reduce performance."
      },
      {
        "name": "tcp_no_new_conn_bflag",
        "detail": "core parameter",
        "doc": "A branch flag used to instruct OpenSIPS not to open a new TCP connection when delivering a request, but only to reuse an existing one, if available. If no existing connection is available, a generic send error is returned."
      },
      {
        "name": "tcp_no_new_conn_rplflag",
        "detail": "core parameter",
        "doc": "A message flag, similar to [tcp_no_new_conn_bflag](#tcp_no_new_conn_bflag), used to prevent OpenSIPS from opening a new TCP connection when sending back a reply for the current request. If no existing connection is available, sending the reply may fail at transport level."
      },
      {
        "name": "tcp_socket_backlog",
        "detail": "core parameter",
        "doc": "The maximum length to which the queue of pending connections for TCP listening sockets may grow. If a connection request arrives when the queue is full, the client may receive an error such as `ECONNREFUSED`; if the underlying protocol supports retransmission, the request may be ignored so a later connection attempt can succeed."
      },
      {
        "name": "tcp_threshold",
        "detail": "core parameter",
        "doc": "A number representing the maximum number of microseconds sending a TCP request is expected to take. Anything above the set number triggers a warning message to the logging facility."
      },
      {
        "name": "tcp_keepalive",
        "detail": "core parameter",
        "doc": "Enables or disables TCP keepalive at OS level."
      },
      {
        "name": "tcp_keepcount",
        "detail": "core parameter",
        "doc": "Number of keepalive probes to send before closing the connection. This option is available on Linux and other platforms with `TCP_KEEPCNT` support. The OS default can usually be found using `cat /proc/sys/net/ipv4/tcp_keepalive_probes`; a common value is `9`."
      },
      {
        "name": "tcp_keepidle",
        "detail": "core parameter",
        "doc": "Amount of idle time, in seconds, before OpenSIPS starts sending keepalive probes. This option is available on Linux and other platforms with `TCP_KEEPIDLE` support. The OS default can usually be found using `cat /proc/sys/net/ipv4/tcp_keepalive_time`; a common value is `7200` seconds."
      },
      {
        "name": "tcp_keepinterval",
        "detail": "core parameter",
        "doc": "Interval, in seconds, between keepalive probes when the previous probe failed. This option is available on Linux and other platforms with `TCP_KEEPINTVL` support. The OS default can usually be found using `cat /proc/sys/net/ipv4/tcp_keepalive_intvl`; a common value is `75` seconds."
      },
      {
        "name": "timer_workers",
        "detail": "core parameter",
        "doc": "The number of worker processes created exclusively for timer-related tasks. The minimum number is `1`."
      },
      {
        "name": "tos",
        "detail": "core parameter",
        "doc": "The TOS (Type Of Service) to be used for the sent IP packets, for both TCP and UDP. The default value is `IPTOS_LOWDELAY`. To disable TOS setting, use `0`."
      },
      {
        "name": "udp_workers",
        "detail": "core parameter",
        "doc": "Number of worker processes to be created for each UDP or SCTP interface. The default value is `8`."
      },
      {
        "name": "user_agent_header",
        "detail": "core parameter",
        "doc": "The body of the User-Agent header field generated by **OpenSIPS** when it sends a request as UAC. It defaults to `OpenSIPS ( (/))`."
      },
      {
        "name": "wdir",
        "detail": "core parameter",
        "doc": "The working directory used by **OpenSIPS** at runtime. If not explicitly configured, **OpenSIPS** changes the working directory to `/`."
      },
      {
        "name": "xlog_buf_size",
        "detail": "core parameter",
        "doc": "Size of the buffer used to print a single line through the selected **OpenSIPS** logging facility. If the buffer is too small, an overflow error will be printed and the line will be skipped. The default value is `4096` bytes."
      },
      {
        "name": "xlog_force_color",
        "detail": "core parameter",
        "doc": "Enables the use of [$C(xy)](Script-CoreVar.md#foreground-and-background-colors) color escape sequences in [xlog()](./Script-CoreFunctions.md#xloglog_level-format_string). Otherwise, color escape sequences have no effect. The default value is `false`."
      },
      {
        "name": "xlog_level",
        "detail": "core parameter",
        "doc": "Similar to [log_level](#log_level), this parameter independently controls the verbosity of the [xlog()](./Script-CoreFunctions.md#xloglog_level-format_string) functions. This allows you to separately control the verbosity level for logs generated by code and logs generated by `xlog()`. The default value is `2` / `L_NOTICE`."
      },
      {
        "name": "xlog_print_level",
        "detail": "core parameter",
        "doc": "Default level for printing logs generated by the [xlog()](./Script-CoreFunctions.md#xloglog_level-format_string) core function when the `log_level` parameter is omitted. The default value is `2` / `L_NOTICE`."
      }
    ],
    "pvars": [
      {
        "name": "$ai",
        "detail": "URI in SIP Request's P-Asserted-Identity header",
        "doc": "`$ai` - reference to URI in request's P-Asserted-Identity header (see RFC 3325)"
      },
      {
        "name": "$adu",
        "detail": "Authentication Digest URI",
        "doc": "`$adu` - URI from Authorization or Proxy-Authorization header. This URI is used when calculating the HTTP Digest Response."
      },
      {
        "name": "$ar",
        "detail": "Authentication realm",
        "doc": "`$ar` - realm from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$au",
        "detail": "Auth username user",
        "doc": "`$au` - user part of username from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$ad",
        "detail": "Auth username domain",
        "doc": "`$ad` - domain part of username from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$an",
        "detail": "Auth nonce",
        "doc": "`$an` - the nonce from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.resp",
        "detail": "Auth response",
        "doc": "`$auth.resp` - the authentication response from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.nonce",
        "detail": "Auth nonce",
        "doc": "`$auth.nonce` - the nonce string from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.cnonce",
        "detail": "Auth cnonce",
        "doc": "`$auth.cnonce` - the client nonce string from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.opaque",
        "detail": "Auth opaque",
        "doc": "`$auth.opaque` - the opaque string from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.alg",
        "detail": "Auth algorithm",
        "doc": "`$auth.alg` - the algorithm string from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.qop",
        "detail": "Auth QOP",
        "doc": "`$auth.qop` - the value of qop parameter from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$auth.nc",
        "detail": "Auth nonce count (nc)",
        "doc": "`$auth.nc` - the value of nonce count parameter from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$aU",
        "detail": "Auth whole username",
        "doc": "`$aU` - whole username from Authorization or Proxy-Authorization header"
      },
      {
        "name": "$Au",
        "detail": "Acc username",
        "doc": "`$Au` - username for accounting purposes. It's a selective pseudo variable (inherited from acc module). It returns `$au` if it exists or From username otherwise."
      },
      {
        "name": "$argv",
        "detail": "Argument options",
        "doc": "`$argv` - provides access to command line arguments specified with '-o' option. Examples:"
      },
      {
        "name": "$challenge.algorithm",
        "detail": "Authorize Challenge Algorithm",
        "doc": "`$challenge.algorithm` - the algorithm value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.realm",
        "detail": "Authorize Challenge Realm",
        "doc": "`$challenge.realm` - the realm value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.nonce",
        "detail": "Authorize Challenge Nonce",
        "doc": "`$challenge.nonce` - the nonce value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.opaque",
        "detail": "Authorize Challenge Opaque",
        "doc": "`$challenge.opaque` - the opaque value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.qop",
        "detail": "Authorize Challenge QOP",
        "doc": "`$challenge.qop` - the qop value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.ik",
        "detail": "Authorize Challenge IK",
        "doc": "`$challenge.ik` - the ik value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$challenge.ck",
        "detail": "Authorize Challenge CK",
        "doc": "`$challenge.ck` - the ck value taken from the WWW-Authorize or Proxy-Authorize header."
      },
      {
        "name": "$ci",
        "detail": "Call-Id",
        "doc": "`$ci` - reference to body of call-id header"
      },
      {
        "name": "$cl",
        "detail": "Content-Length",
        "doc": "`$cl` - reference to body of content-length header"
      },
      {
        "name": "$cs",
        "detail": "CSeq number",
        "doc": "`$cs` - reference to cseq number from cseq header"
      },
      {
        "name": "$ct",
        "detail": "Contact instance",
        "doc": "`$ct` - reference to contact instance/body from the contact header. A contact instance is  display_name + URI + contact_params. As a Contact header may contain multiple Contact instances and a message may contain multiple Contact headers, an index was added to the `$ct` variable: * `$ct` -first contact instance from message * `$(ct[n])` - the n-th contact instance form the beginning of message, starting with index 0 * `$(ct[-n])` - the n-th contact instance form the end of the message, starting with index -1 (the last contact instance)"
      },
      {
        "name": "$ct.fields(field)",
        "detail": "Fields of a contact instance",
        "doc": "`$ct.fields()` - reference to the fields of a contact instance/body (see above). Supported fields are: * name - display name * uri - contact uri * q  - q param (value only) * expires - expires param (value only) * methods - methods param (value only) * received - received param (value only) * params - all params (including names)"
      },
      {
        "name": "$cT",
        "detail": "Content-Type",
        "doc": "`$cT` - reference to body of Content-Type header and also the content-type headers inside a multi-part body * `$cT` - the main Content-Type of the message; the one inside the headers * `$(cT[n])` - the **n**-th Content-Type inside a multi-part body from the beginning of message, starting with index 0 * `$(cT[-n])` - the **n**-th Content-Type inside a multi-part body from the end of the message, starting with index -1 (the last contact instance) * `$(cT[*])` - all the Content-Type headers including the main one and the ones from the multi-part body"
      },
      {
        "name": "$dd",
        "detail": "Domain of destination URI",
        "doc": "`$dd` - reference to domain of destination uri"
      },
      {
        "name": "$di",
        "detail": "Diversion header URI",
        "doc": "`$di` - reference to Diversion header URI"
      },
      {
        "name": "$dip",
        "detail": "Diversion \"privacy\" parameter",
        "doc": "`$dip` - reference to Diversion header \"privacy\" parameter value"
      },
      {
        "name": "$dir",
        "detail": "Diversion \"reason\" parameter",
        "doc": "`$dir` - reference to Diversion header \"reason\" parameter value"
      },
      {
        "name": "$dp",
        "detail": "Port of destination URI",
        "doc": "`$dp` - reference to port of destination uri"
      },
      {
        "name": "$dP",
        "detail": "Transport protocol of destination URI",
        "doc": "`$dP` - reference to transport protocol of destination uri"
      },
      {
        "name": "$ds",
        "detail": "Destination set",
        "doc": "`$ds` - reference to destination set"
      },
      {
        "name": "$du",
        "detail": "Destination URI",
        "doc": "`$du` - reference to destination uri (outbound proxy to be used for sending the request) If loose_route() returns TRUE a destination uri is set according to the first Route header."
      },
      {
        "name": "$err.class",
        "detail": "Error class",
        "doc": "`$err.class` - the class of error (now is '1' for parsing errors)"
      },
      {
        "name": "$err.level",
        "detail": "Error level",
        "doc": "`$err.level` - severity level for the error"
      },
      {
        "name": "$err.info",
        "detail": "Error info",
        "doc": "`$err.info` - text describing the error"
      },
      {
        "name": "$err.rcode",
        "detail": "Error reply code",
        "doc": "`$err.rcode` - recommended reply code"
      },
      {
        "name": "$err.rreason",
        "detail": "Error reply reason",
        "doc": "`$err.rreason` - recommended reply reason phrase"
      },
      {
        "name": "$fd",
        "detail": "From URI domain",
        "doc": "`$fd` - reference to domain in URI of 'From' header"
      },
      {
        "name": "$fn",
        "detail": "From display name",
        "doc": "`$fn` - reference to display name of 'From' header"
      },
      {
        "name": "$ft",
        "detail": "From tag",
        "doc": "`$ft` - reference to tag parameter of 'From' header"
      },
      {
        "name": "$fu",
        "detail": "From URI",
        "doc": "`$fu` - reference to URI of 'From' header"
      },
      {
        "name": "$fU",
        "detail": "From URI username",
        "doc": "`$fU` - reference to username in URI of 'From' header"
      },
      {
        "name": "$log_level",
        "detail": "OpenSIPS Log level",
        "doc": "`$log_level` - changes the log level for the current process ; the log level can be set to a new value (see [possible values](Script-CoreParameters.md#log_level) or it can be reset back to the global log level. This function is very helpful if you are tracing and debugging only a specific piece of code."
      },
      {
        "name": "$mb",
        "detail": "SIP message buffer",
        "doc": "`$mb` - reference to SIP message buffer"
      },
      {
        "name": "$mf",
        "detail": "Message Flags",
        "doc": "`$mf` - displays a list with the message/transaction flags set for the current SIP request"
      },
      {
        "name": "$mi",
        "detail": "SIP message ID",
        "doc": "`$mi` - reference to SIP message id"
      },
      {
        "name": "$ml",
        "detail": "SIP message length",
        "doc": "`$ml` - reference to SIP message length"
      },
      {
        "name": "$msg.branch",
        "detail": "Message branch",
        "doc": "`$msg.branch` - this variable is used for creating new message branches by writing into it the value of a SIP URI. By reading this variable, you get the SIP URI of the current/last added branch (or of the RURI branch if no additional branch was added so far)."
      },
      {
        "name": "$msg.branch.uri",
        "detail": "SIP URI of a message branch",
        "doc": "`$msg.branch.uri` -  gives read / write access over the SIP URI (as string) of an existing message branch. The message branches are created via [append_msg_branch()](Script-CoreFunctions.md#append_msg_branchuri-qvalue-flags) core function or by various modules (like \"registrar\" module). The message branches are consumed by the TM \"t_relay()\" function (they are converted to TM branches)."
      },
      {
        "name": "$msg.branch.duri",
        "detail": "Destination URI of a message branch",
        "doc": "`$msg.branch.duri` -  100% similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating with the Destination-URI value of the message branch."
      },
      {
        "name": "$msg.branch.path",
        "detail": "PATH of a message branch",
        "doc": "`$msg.branch.path` -  100% similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating with the PATH value of the message branch."
      },
      {
        "name": "$msg.branch.q",
        "detail": "Q of a message branch",
        "doc": "`$msg.branch.q` -  100% similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating with the Q value of the message branch."
      },
      {
        "name": "$msg.branch.flags",
        "detail": "Flags of a message branch",
        "doc": "`$msg.branch.flags` -  100% similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating with list (comma separated) of per-branch flags (which are set for the branch)."
      },
      {
        "name": "$msg.branch.socket",
        "detail": "SIP socket of a message branch",
        "doc": "`$msg.branch.socket` -  100% similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating with the (forced) socket value of the message branch."
      },
      {
        "name": "$msg.branch.flag()",
        "detail": "A flag of a message branch",
        "doc": "`$msg.branch.flag()` -  similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating over a single branch flag (for the current branch)."
      },
      {
        "name": "$msg.branch.attr()",
        "detail": "An attribute of a message branch",
        "doc": "`$msg.branch.attr()` -  similar to [`$msg.branch.uri`](#sip-uri-of-a-message-branch---msgbranchuri), but operating over a single branch attribute (attached to the current branch)."
      },
      {
        "name": "$msg.branch.last_idx",
        "detail": "Index of the last message branch",
        "doc": "`$msg.branch.last_idx` -  returns the index of the last message branch. IF no additional branches were added, it will return 0, the index of the RURI branch. Then the returned value will get incremented with each append_msg_branch()."
      },
      {
        "name": "$msg.flag",
        "detail": "Message flag",
        "doc": "`$msg.flag(flag_name)` - this variable provides read/write access to the value of a single certain message flag (identified by name). The values accepted for writing are 1 (set) and 0 (unset). The returned values are 1/\"true\" (set) and 0/\"false\" (unset)."
      },
      {
        "name": "$msg.is_request",
        "detail": "Message is request",
        "doc": "`$msg.is_request` - this variable tells if the current SIP message is a request or not. The returned values are 1/\"true\" (request) and 0/\"false\" (reply)."
      },
      {
        "name": "$msg.type",
        "detail": "Message type",
        "doc": "`$msg.type` - this variable returns the type of the current  message. The returned values are \"request\" (request) or \"reply\" (reply)."
      },
      {
        "name": "$od",
        "detail": "Domain in SIP Request's original URI",
        "doc": "`$od` - reference to domain in request's original R-URI"
      },
      {
        "name": "$op",
        "detail": "Port of SIP request's original URI",
        "doc": "`$op` - reference to port of original R-URI"
      },
      {
        "name": "$oP",
        "detail": "Transport protocol of SIP request original URI",
        "doc": "`$oP` - reference to transport protocol of original R-URI"
      },
      {
        "name": "$ou",
        "detail": "SIP Request's original URI",
        "doc": "`$ou` - reference to request's original URI"
      },
      {
        "name": "$oU",
        "detail": "Username in SIP Request's original URI",
        "doc": "`$oU` - reference to username in request's original URI"
      },
      {
        "name": "$path",
        "detail": "Path header",
        "doc": "`$path` - reference to the Path header body."
      },
      {
        "name": "$param",
        "detail": "Route parameter",
        "doc": "`$param(idx)` - retrieves the parameters of the route. The index can be an integer, or a pseudo-variable (index starts at 1)."
      },
      {
        "name": "$pd",
        "detail": "Domain in SIP Request's P-Preferred-Identity header URI",
        "doc": "`$pd` - reference to domain in request's P-Preferred-Identity header URI (see RFC 3325)"
      },
      {
        "name": "$proxy_protocol",
        "detail": "Proxy Protocol from transport layer",
        "doc": "`$proxy_protocol(field)` - retrieves Proxy Protocol information from the transport layer. Supported fields are: **src_ip**, **src_port**, **dst_ip**, **dst_port**, **af**"
      },
      {
        "name": "$pn",
        "detail": "Display Name in SIP Request's P-Preferred-Identity header",
        "doc": "`$pn` - reference to Display Name in request's P-Preferred-Identity header (see RFC 3325)"
      },
      {
        "name": "$pp",
        "detail": "Process id",
        "doc": "`$pp` - reference to process id (pid)"
      },
      {
        "name": "$pU",
        "detail": "User in SIP Request's P-Preferred-Identity header URI",
        "doc": "`$pU` - reference to user in request's P-Preferred-Identity header URI (see RFC 3325)"
      },
      {
        "name": "$pu",
        "detail": "URI in SIP Request's P-Preferred-Identity header",
        "doc": "`$pu` - reference to URI in request's P-Preferred-Identity header (see RFC 3325)"
      },
      {
        "name": "$rd",
        "detail": "Domain in SIP Request's URI",
        "doc": "`$rd` - reference to domain in request's URI"
      },
      {
        "name": "$rb",
        "detail": "Body of request/reply",
        "doc": "`$rb` - reference to the body or a body part of the SIP message * `$rb` - the whole body of the message (with all the parts) * `$(rb[*])` - same as `$rb` * `$(rb[n])` - the n-th body belonging to a multi-part body from the beginning of message, starting with index 0 * `$(rb[-n])` - the n-th body belonging to a multi-part body from the end of the message, starting with index -1 (the last contact instance) * `$rb(application/sdp)`   - get the first SDP body part * `$(rb(application/isup)[-1])`  - get the last ISUP body part"
      },
      {
        "name": "$rc",
        "detail": "Returned code",
        "doc": "`$rc` - reference to returned code by last invoked function"
      },
      {
        "name": "$re",
        "detail": "Remote-Party-ID header URI",
        "doc": "`$re` - reference to Remote-Party-ID header URI"
      },
      {
        "name": "$return",
        "detail": "Return value",
        "doc": "`$return` - Returns the value of the previously executed route."
      },
      {
        "name": "$rm",
        "detail": "SIP request's method",
        "doc": "`$rm` - reference to request's method"
      },
      {
        "name": "$rp",
        "detail": "SIP request's port",
        "doc": "`$rp` - reference to port of R-URI"
      },
      {
        "name": "$rP",
        "detail": "Transport protocol of SIP request URI",
        "doc": "`$rP` - reference to transport protocol of R-URI"
      },
      {
        "name": "$rr",
        "detail": "SIP reply's reason",
        "doc": "`$rr` - reference to reply's reason"
      },
      {
        "name": "$rs",
        "detail": "SIP reply's status",
        "doc": "`$rs` - reference to reply's status"
      },
      {
        "name": "$rt",
        "detail": "Refer-to URI",
        "doc": "`$rt` - reference to URI of refer-to header"
      },
      {
        "name": "$ru",
        "detail": "SIP Request's URI",
        "doc": "`$ru` - reference to request's URI"
      },
      {
        "name": "$rU",
        "detail": "Username in SIP Request's URI",
        "doc": "`$rU` - reference to username in request's URI"
      },
      {
        "name": "$ru_q",
        "detail": "Q value of the SIP Request's URI",
        "doc": "`$ru_q` - reference to q value of the R-URI"
      },
      {
        "name": "$sdp",
        "detail": "SDP body",
        "doc": "`$sdp` - Read/Write reference to the SDP body of the current SIP message"
      },
      {
        "name": "$sdp.line",
        "detail": "SDP body line",
        "doc": "`$sdp.line` - Read/Write reference to SDP body lines, with filtering support"
      },
      {
        "name": "$sdp.stream",
        "detail": "SDP body line",
        "doc": "`$sdp.stream` - Read/Write reference to SDP body streams, with filtering support"
      },
      {
        "name": "$sdp.session",
        "detail": "SDP body session",
        "doc": "`$sdp.session` - Read/Write reference to the SDP body session, with filtering support"
      },
      {
        "name": "$sdp.stream.idx",
        "detail": "SDP stream index",
        "doc": "`$sdp.stream.idx` - Read-Only reference to the index of the matched SDP stream.  Yields NULL on no-match."
      },
      {
        "name": "$si",
        "detail": "IP source address",
        "doc": "`$si` - reference to IP source address of the message"
      },
      {
        "name": "$socket_in / $socket_in(field)",
        "detail": "Socket inbound",
        "doc": "`$socket_in` - read-only variable to get the description (proto:ip:port format) of the inbound socket (used for receiving the message)."
      },
      {
        "name": "$socket_out / $socket_out(field)",
        "detail": "Socket outbound",
        "doc": "`$socket_out` - read-write variable for reading or changing the outbound socket of the message. Originally (before being written/changed) it will return the same socket description as [`$socket_in`](#socket-inbound---socket_in--socket_infield) (the inbound socket will be used as outbound socket also). In addition, it also supports the `forced` sub-field, which returns a socket description only if a socket had been explicitly forced; thus, as opposed to the regular [`$socket_out`](#socket-outbound---socket_out--socket_outfield), if no socket had explicitly been forced, the variable returns NULL."
      },
      {
        "name": "$sp",
        "detail": "Source port",
        "doc": "`$sp` - reference to the source port of the message"
      },
      {
        "name": "$td",
        "detail": "To URI Domain",
        "doc": "`$td` - reference to domain in URI of 'To' header"
      },
      {
        "name": "$tn",
        "detail": "To display name",
        "doc": "`$tn` - reference to display name of 'To' header"
      },
      {
        "name": "$tt",
        "detail": "To tag",
        "doc": "`$tt` - reference to tag parameter of 'To' header"
      },
      {
        "name": "$tu",
        "detail": "To URI",
        "doc": "`$tu` - reference to URI of 'To' header"
      },
      {
        "name": "$tU",
        "detail": "To URI Username",
        "doc": "`$tU` - reference to username in URI of 'To' header"
      },
      {
        "name": "$time",
        "detail": "Formatted date and time",
        "doc": "`$time(format)` - returns the string formatted time according to UNIX date (see: **man date**)."
      },
      {
        "name": "$T_branch_idx",
        "detail": "Branch index",
        "doc": "`$T_branch_idx` - the index (starting with 1 for the first branch) of the branch for which is executed the branch_route[]. If used outside of branch_route[] block, the value is '0'. This is exported by TM module."
      },
      {
        "name": "$Tf",
        "detail": "String formatted time",
        "doc": "`$Tf` - reference string formatted time"
      },
      {
        "name": "$Ts",
        "detail": "Current unix time stamp in seconds",
        "doc": "`$Ts` - reference to current unix time stamp in seconds"
      },
      {
        "name": "$Tsm",
        "detail": "Current microseconds of the current second",
        "doc": "`$Tsm` - reference to current microseconds of the current second"
      },
      {
        "name": "$TS",
        "detail": "Startup unix time stamp",
        "doc": "`$TS` - reference to startup unix time stamp"
      },
      {
        "name": "$ua",
        "detail": "User agent header",
        "doc": "`$ua` - reference to user agent header field"
      },
      {
        "name": "$hdr",
        "detail": "SIP Headers",
        "doc": "`$(hdr(name)[N])` - represents the body of the N-th header identified by 'name'. If [N] is omitted then the body of the first header is printed. The first header is retrieved when N=0, for the second N=1, and so on. To print the last header of that type, use -1, no other negative values are supported now. No white spaces are allowed inside the specifier (before `}`, before or after `{`, [, ] symbols). When N='*', all headers of that type are printed."
      },
      {
        "name": "$route",
        "detail": "Route Name (Full)",
        "doc": "`$route` - Access route names of the current route call stack.  Usage examples (assuming a route call stack of \"route > route[A] > route[B]\"):"
      },
      {
        "name": "$route.type",
        "detail": "Route Type",
        "doc": "`$route.type` - Access the type of the current route.  May be indexed, using positive or negative indexes."
      },
      {
        "name": "$route.name",
        "detail": "Route Name",
        "doc": "`$route.name` - Access the name of the current route.  May be indexed, using positive or negative indexes."
      },
      {
        "name": "$cfg_line",
        "detail": "Current script line and file",
        "doc": "`$cfg_line` - Holds the current line from the script of the action being executed, useful for logging purposes"
      },
      {
        "name": "$xlog_level",
        "detail": "Log level for xlog()",
        "doc": "`$xlog_level` - allows to set /reset the xlog() logging level on per-process bases. Shortly said, you can read the verbosity level for the xlog() calls or you can temporary change the level per process bases."
      }
    ],
    "statements": [
      {
        "name": "if",
        "detail": "control statement",
        "doc": "IF-ELSE statement"
      },
      {
        "name": "switch",
        "detail": "control statement",
        "doc": "SWITCH statement - it can be used to test the value of a pseudo-variable."
      },
      {
        "name": "while",
        "detail": "control statement",
        "doc": "while statement"
      },
      {
        "name": "for",
        "detail": "control statement",
        "doc": "for each statement - easy iteration over indexed variables or pseudo-variables"
      },
      {
        "name": "else",
        "detail": "control statement — documented under `if`",
        "doc": "IF-ELSE statement"
      },
      {
        "name": "case",
        "detail": "control statement — documented under `switch`",
        "doc": "SWITCH statement - it can be used to test the value of a pseudo-variable."
      },
      {
        "name": "default",
        "detail": "control statement — documented under `switch`",
        "doc": "SWITCH statement - it can be used to test the value of a pseudo-variable."
      },
      {
        "name": "async",
        "detail": "control statement",
        "doc": "The **async()** statement of the OpenSIPS script can be used in situations where the script writer both needs to perform blocking I/O and also depends on the result of this operation. Some example scenarios:"
      },
      {
        "name": "launch",
        "detail": "control statement",
        "doc": "The **launch()** statement of the OpenSIPS script can be used in situations where the script writer needs to perform blocking I/O, but does not depend on the result of this operation in order to continue the current SIP routing decision flow. Some example scenarios:"
      }
    ],
    "routes": [
      {
        "name": "route",
        "detail": "route type",
        "doc": "Request routing block. It contains a set of actions to be taken for SIP requests."
      },
      {
        "name": "branch_route",
        "detail": "route type",
        "doc": "Request's branch routing block. It contains a set of actions to be taken for each branch of a SIP request."
      },
      {
        "name": "failure_route",
        "detail": "route type",
        "doc": "Failed transaction routing block. It contains a set of actions to be taken each transaction that received only negative replies (>=300) for all branches."
      },
      {
        "name": "onreply_route",
        "detail": "route type",
        "doc": "Reply routing block. It contains a set of actions to be taken for SIP replies."
      },
      {
        "name": "error_route",
        "detail": "route type",
        "doc": "The error route is executed automatically when a parsing error occurs during SIP request processing, or when a script [assert](./Script-CoreFunctions.md#assertstatement-description) fails. It allows the administrator to decide what to do in such error cases."
      },
      {
        "name": "local_route",
        "detail": "route type",
        "doc": "The local route is executed automatically when a new SIP request is generated by TM, internally (no UAC side). This is a route intended to be used for message inspection, accounting and for applying last changes on the message headers. Routing and signaling functions are not allowed."
      },
      {
        "name": "startup_route",
        "detail": "route type",
        "doc": "The **startup_route** is executed only once when OpenSIPS is started and before the processing of SIP messages begins. This is useful if some initiation actions are needed, like loading some data in the cache, to ease up the future processing. Notice that this route, compared to the others is not triggered at the receipt of a message, so the functions that can be called here must not do processing on the message."
      },
      {
        "name": "timer_route",
        "detail": "route type",
        "doc": "The **timer_route** is a route executed periodically at a configured interval of time specified next to the name (in seconds). Similar to *startup_route*, this route does not process a SIP message.  Multiple timer routes (possibly at differing running intervals) are allowed."
      },
      {
        "name": "event_route",
        "detail": "route type",
        "doc": "The **event_route** is used by the OpenSIPS Event Interface to execute script code when an event is triggered. The name of the route is the event that has to be handled by that route. The route itself is executed asynchronously with regards to the trigger moment."
      }
    ]
  }
}
